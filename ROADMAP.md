# ROADMAP — интерактивная лаборатория «Мухолет»

Журнал ночных доработок. Каждый запуск автоматизации: (1) прочитай этот файл,
(2) выбери верхний невыполненный пункт, (3) реализуй, (4) проверь
(`.venv/bin/python -m pytest -q`, `cd web && npx tsc --noEmit && npm run build`,
смоук в браузере), (5) запиши под этим файлом отчёт «что сделано / что сломалось /
что дальше». Не ломать работающее. Не задавать вопросов — решать самостоятельно.

## Как запускать и проверять
- API: `.venv/bin/python -m uvicorn navedenie.app:app --port 8091 --host 127.0.0.1`
- UI: `cd web && npm run dev` (Vite на 5173, прокси /api → 8091)
- Тесты: `.venv/bin/python -m pytest -q` (18 passed — база)
- Сборка: `cd web && npx tsc --noEmit && npm run build`
- Стек: FastAPI + numpy (БЕЗ scipy), React + three.js. Статика раздаётся Vite в dev.

## Архитектура (коротко)
- `navedenie/sim.py` — точка-масса 3D, сценарии, СК: x=дальность, y=бок, z=высота.
- `navedenie/engine.py` — цикл прогона: ГСН → (ПН | био) → интеграция.
- `navedenie/circuit.py` — мозг дрозофилы: stub (87), full (4503), connectome (~139 тыс,
  связь через базисы 192, seed 42). Обучается только W_dn (2×8 / 2×32).
- `navedenie/swarm.py` — рой: геномы bio (W_dn+gain) и pn (N), эволюция, медиана
  по сценариям поколения, телеметрия tel[] для анимации.
- `navedenie/train.py` — пошаговое обучение: train_start/step/finish, телеметрия.
- `web/src/` — App.tsx (панели), EngagementView.tsx (3D: геометрия наведения,
  самолёт, bloom, взрыв), Brain3DView.tsx (точечный мозг), ui.tsx (муха),
  localSim.ts (фолбэк «в окне»), swarm.ts (рой клиент+сервер).
- Тема день/ночь, тултипы на всём, уроки 1–4, порт API 8091 (8090 занят!).

## Бэклог (по приоритету)

### 1. Docker + офлайн amd64 (КРИТИЧНО, сделать первым)
- Multi-stage Dockerfile: stage1 node:20-alpine → build web (npm ci + tsc + vite build);
  stage2 python:3.12-slim → venv, зависимости ТОЛЬКО из локальных wheel
  (`pip download -r <deps> -d wheels/` на этапе сборки хоста, в контейнер
  `pip install --no-index --find-links /wheels`).
- FastAPI раздаёт `web/dist` как статику (app.mount, StaticFiles) → один контейнер,
  один порт (8080), полностью офлайн. Vite base: './'.
- docker-compose.yml + README: `docker build -t muholet . && docker run -p 8080:8080`.
- Проверить: `docker run` → открыть страницу без интернета, пуск/рой работают.
- ВАЖНО: системное время может отличаться — ничего от времени не зависит.

### 2. Экран метрик обучения и прогона
- Отдельная вкладка/оверлей «Лаборатория»: графики (canvas, без библиотек):
  промах по эпизодам обучения, W_dn по эпохам (16 чисел), промах по поколениям роя
  (лучший/медиана), гистограмма ZEM.
- История в памяти клиента + кнопка «экспорт CSV».

### 3. Шум и несовершенство измерителей (настройки)
- В Scenario + UI: σ шума пеленга (рад), σ шума дальности, дрейф нуля ГСН,
  случайные пропуски захвата (вероятность на кадр), джиттер задержки ГСН.
- Влияет на obs в seeker.observe; честно отображать в телеметрии.
- Тесты: шум 0 → поведение как сейчас; большой шум → промах растёт.

### 4. Разнообразие траекторий для роя/обучения (робастность)
- Расширить `sample_generation_scenario` / `_episode_scenario`:
  змейка с переменной частотой и амплитудой, форсированный вираж (break turn),
  «ножницы», пикирование/горка (изменение высоты), барражирование,
  комбо (вираж + змейка), манёвр уклонения в последние 2 с.
- Манёвры цели — новые типы в `sim.py::target_accel` (не ломать существующие).
- Рой должен видеть ЭТИ траектории и в 3D (трек цели уже рисуется).
- Тесты: каждая новая траектория детерминирована, rollout не падает.

### 5. Сравнение законов наведения
- Законы: PPN (есть), APN (учёт ускорения цели), чистое преследование,
  коннектом/схема (bio), 3-точечный (CLOS). Реализация в `navedenie/pn.py`.
- В «сравнить мозги» добавить строки законов; в 3D — переключатель закона для пуска.
- Таблица: промах/перехват/пик g/захват/время.

### 6. Анимация мозга — нагляднее (приоритет пользователя!)
- Сейчас точки слишком тусклые/незаметные. Сделать: частицы-импульсы бегут
  ПО ЛИНИЯМ ПРОВОДКИ (LINKS) с интенсивностью от активности источника;
  пульсация регионов; кнопка «во весь экран»; опция увеличенного размера точек.
- Честность: любая анимация = данные из кадра, никаких таймеров.

### 7. Полировка визуала
- Дымный след цели при манёвре, трассер мухолета, настраиваемый bloom,
  звёзды/облачный слой днём.

### 8. Мелочи
- Сохранение настроек сценария в localStorage.
- Горячие клавиши: Пробел = пуск, S = стоп.

## Отчёты запусков
(каждый запуск добавляет запись ниже)
- [2026-09-16 вечер, человек] v1 лаборатории: мозг-коннектом, геометрия наведения,
  рой с разными геометриями, взрыв самолёта, день/ночь, сохранение мозгов,
  сравнение мозгов, уроки. База: 18 тестов.

### Запуск — 2026-09-21: адаптивный BIO без времени, фазовые признаки theta/rho, N_eff, профили скорости
**Сделано: исследовательская серия «адаптивность закона наведения» (P0+P1+P2).**

Схема признаков v2 (FEAT_DIM 8→10):
- seeker.py: raster_foveal — планка ширины пятна ЛОКАЛЬНАЯ (0.7·ячейки вместо 0.25·max всей сетчатки):
  в фовеа пятно узкое — угловой размер цели разрешим с ~580 м (12-м цель, Vc 1040), на периферии — крупный.
  measure_theta: обратная задача оптики (бисекция T(σ)=яркость в известной модели своей сетчатки) → theta рад;
  theta→0, пока цель не разрешима PSF (честный ответ «не измеримо»).
- observe: сглаживание theta (эксп. фильтр 60 мс, «динамика фоторецептора»), θ̇ на окне 6 кадров
  (окно едет внутри кадра через буфер задержки), rho = θ̇/θ (клип −10…30 1/с), tau_contact = θ/θ̇.
  После транзита появления цели (θ>0.05) corr(rho, Vc/R) = 0.98 — оптическая 1/t_go без истинной дальности.
- circuit._features: [3β, 3λ, 0.4ωβ, 0.4ωλ, 4θ, loom, 0.4ρ, 4Φx, 4Φy, захват]; K_THETA=4, K_RHO=0.4
  подобраны по фактическим диапазонам (theta 0…0.45 рад, rho 0…8 1/с в операционной зоне).
- Новые каналы az_dot_s/el_dot_s (фильтр 0.1 с) — ТОЛЬКО для углоскоростных законов (pn_gsn,
  pn_sched_sensor); признаки контура не тронуты. pn_gsn больше не живёт в насыщении.

Версионирование и миграция: FEATURE_SCHEMA_VERSION=2 в NPZ (feat_schema); migrate_feature_columns
расширяет W_dn/W_k/W_fx нулями на столбцах 4 (theta) и 6 (rho) — политика до дообучения не меняется.
Схема новее стенда → load()=False. Старые геномы роя (16 весов) и локальный brain.ts мигрируют так же.

N_eff (pn.n_eff_from): N_eff = ⟨a,q⟩/⟨q,q⟩, q = perp(ω×V,V) на ИСТИННОЙ геометрии — постфактум-диагностика,
в управление не входит. Невалидна при |q|<0.5 м/с² (ω≈0), насыщении ≥0.98·n_max·g, нечисловых значениях.
Поля Frame/RunResult: n_eff, n_eff_valid/reason, n_eff_yaw/pitch, sat, tgo; агрегаты median/q25/q75/min/max,
valid_frac, sat_frac, corr(N_eff,ρ), corr(N_eff,t_go). У oracle-ПН N_eff ≡ pn_n (IQR=0).

Профили скорости цели: Scenario += target_speed_mode (constant/accelerate/decelerate/pulse/sine),
target_longitudinal_g, speed_min/max, period_s, phase; integrate_target: вираж + продольное изменение
модуля в границах, трапеция; constant = прежнее поведение в точности. ЧЕСТНЫЙ v_t: убран скрытый ×0.85
в tail-chase (аспект меняет направление, не модуль). Протокол DR — train.PROTOCOL (распределения + seeds
train 11+7ep / validation — каноническое трио / test 9000+ячейка); ~45% эпизодов с профилями.

Scheduled PN — отдельный контрольный закон (не замена ПН): N=clip(N0+kρ·ρ,N_min,N_max); варианты
pn_sched_oracle (истинное Vc/R) и pn_sched_sensor (измеренное ρ + сглаженные ω). Классическая ПН — baseline.

Полином: переименован в «Полиномиальный суррогат мгновенного readout по текущим сенсорным признакам»
(без состояний/tau/задержек/истории — написано в шапке экспортов). Основной фит — flight-domain:
признаки реальных прогонов протокола, разбиение ПО ЭПИЗОДАМ (6/2, seed 77), R²/MAE/max на held-out,
ошибки центр/периферия и малое/большое rho, диапазоны признаков; глобальная сетка — stress-test.
Локальные усиления K_az=∂yaw/∂ω, K_el=∂pitch/∂ω и N_poly_eff=K·0.4·n_max·g/V_m — ТОЛЬКО при явной V_m
(таблица 650/780/950). C-экспорт: feat[10], dn_local_gain.

Наука: science.speed_matrix (held-out матрица v_m×v_t×аспект×профиль×манёвр), neff_report (адаптивность),
feature_ablation (none/no_theta/no_rho/no_both/rho_fixed/rho_perm через feat_patch). API: /api/science/*.
UI: панель «Условия пуска» — профиль скорости + параметры scheduled PN; «Показатели» — θ, ρ, τ, скорость
и профиль цели, N_eff со статусом, насыщение; лаборатория — вкладка «N_eff» (N_eff(t/t_go/ρ/Vc), сводка
медиана/IQR/валидность/насыщение/корреляция, распределение по профилям, CSV); CSV траекторий — t, R, V_m,
V_t, Vc, θ, θ̇, ρ, t_go, az/el, ω, N_eff+валидность, n_req, насыщение, hit/CPA + шапка с версиями
model/metrics(v3)/feature_schema. Локальный TS-стек (types/brain/localSim/metrics/swarm) — зеркало v2.

Численный эксперимент (head-on 8 км, dt 0.02, v_t 150/260/360, профили constant/accel/decel/pulse):
| Закон | hit | CPA мед | effort | N_eff мед | IQR | corr(N_eff,t_go) |
|---|---|---|---|---|---|---|
| ПН (N=4, oracle) | 12/12 | 35.5 | 8.6 | 4.00 | 0.00 | — (константа) |
| scheduled PN (oracle) | 12/12 | 36.4 | 9.7 | 3.19 | 0.22 | −0.68 (N растёт к контакту) |
| scheduled PN (сенсорная) | 0/12 | 187.6 | 7.2 | 0.69 | 0.29 | +0.66 |
| BIO до обучения | 0/12 | 53.2 | 42.6 | 0.88 | 0.35 | +0.15 |
| BIO обученный (мигрирован) | 12/12 | 33.4 | 43.1 | 1.64 | 22.2 | ≈0 |
Мигрированный коннектом на каноническом трио: 37.7 м / 67% (до обновления 44.3 м / 67% — навык сохранён).
Абляции на v2-обученной схеме (CEM-копия): rho_perm 5507 м и 0% (перестановка ρ разрушает политику),
rho_fixed 0.8 → 0%, no_rho — усилие падает вдвое → после дообучения мозг причинно использует ρ;
у мигрированных мозгов столбцы θ/ρ нулевые — абляции совпадают с baseline (ожидаемо, доказательство миграции).
neff_report (мигрированный коннектом): N_eff НЕ постоянен (IQR медиан 1.31 при медиане 0.66), зависимости
от ρ нет (corr≈0), но есть сильная зависимость от v_m (650→0.00, 950→0.78) и v_t (150→0.66, 360→0.98):
политика — не постоянная и не «запланированная» ПН, а контекстно-зависимый закон по скоростному режиму.
На sine (не в матрице испытаний) метрики держатся — обобщение есть.

**Проверка:** pytest 135 passed; tsc + npm run build зелёные; C-экспорт компилируется и совпадает с python.
**Ограничения/дальше:** мозги в data/ обучены на v1-сенсоре (столбцы θ/ρ нулевые) — для адаптивности по фазе
нужно дообучение «по результату» на v2-батче; фитнес CEM только по CPA может раздувать усилие (штраф 0.02·g
слаб); локальная планка пятна немного сместила декодированные пеленги (поведение migrated-мозгов близкое,
но не побитово равное); скорость ракеты в полином не входит — N_poly_eff только при явной V_m.

### Проверка редизайна и целостности диагностики закона — 2026-09-21

**UI:** проверены сохранённые кадры 1920×1080 и 1366×768 и живой интерфейс в обеих темах. Четыре
workspace, инспектор, сгруппированная лаборатория и прокрутка на Full HD работают без горизонтального
переполнения. Canvas-графики переведены с подписей 11–12 px на 12–13 px, увеличены поля и кнопки
PNG/CSV; длинные заголовки получают многоточие вместо жёсткого обрезания. Кэш палитры теперь привязан
к CSS-классу темы: при день↔ночь график сразу получает новые токены, без остаточных цветов старой темы.

**Математика/диагностика:** NARX H5 больше не склеивает последний кадр одного полёта с первым кадром
следующего — лаги `x[t-1]`/`a[t-1]` обнуляются на каждой границе эпизода, текущий `x[t]` сохраняется.
Исправлен счёт параметров H5: матрица `(24×3)` содержит 72 параметра, а не 216. H1/H2/H5 помечаются
как deployable-сенсорные формы, H3/H4 — только как диагностические геометрические модели.

**API:** `/api/science/law-hypotheses` больше не пытается сериализовать внутреннюю `ndarray coef_narx`
(раньше HTTP 500); коэффициенты остаются внутри `/api/science/closed-loop`. Живой smoke: 7 эпизодов,
winner по покадровому unexplained — H5 (R²=0.3185), но closed-loop H5: 0% / CPA 270 м; полином-readout:
67% / CPA 15.3 м, BIO: 67% / CPA 10.9 м. Это подтверждает главный критерий: высокий R² сам по себе
не делает аппроксимацию рабочим законом наведения.

**Проверка:** `npm run build` зелёный; целевые регрессии 3/3; полный pytest **168 passed**. Остаётся
предупреждение Vite о монолитном JS-бандле 937 КБ (gzip 276 КБ) — функциональный дефект отсутствует,
но следующий UI-шаг: lazy-load Three.js рабочих пространств и тяжёлых экранов лаборатории.

### Запуск 1 — 2026-09-17 00:04…00:2x (ночная смена)
**Сделано: пункт 1 «Docker + офлайн amd64».**
- `Dockerfile` — hermetic multi-stage (node:20-alpine build → python:3.12-slim), `Dockerfile.offline` — полностью офлайн (без npm-реестра).
- `navedenie/app.py`: FastAPI раздаёт `web/dist` (mount «/» ПОСЛЕ маршрутов — иначе перекрывает API; в dev не мешает).
- Шрифты ушли из Google Fonts в `web/public/fonts/` (woff2 latin, Chakra Petch 400/500/600/700 + Share Tech Mono; кириллица у этих семейств и раньше рендерилась системным фолбэком) — интернет при работе не нужен.
- `wheels/` — 20 wheel linux/amd64 py3.12 (25 МБ, в .gitignore; `make wheels` пересоздаёт).
- `docker-compose.yml` (name: muholet, platform linux/amd64, том data/), `Makefile`: wheels / web-build / docker-image / docker-offline / docker-run. README-раздел «Docker».
- Проверено на этой машине (Docker Desktop arm64, эмуляция amd64): офлайн-образ собран, контейнер на 8095: страница+шрифты 200, /api/health, пуск (перехват 42.0 м, 145 кадров), /api/brain/compare (ПН 44.8 / схема 564.8 / коннектом 831.5 — свежий контейнер, мозги не обучены). pytest 18 passed, tsc+build зелёные.

**Не сделано / следующему запуску:**
1. Multi-stage `make docker-image` (npm ci в qemu) НЕ проверен до конца — проверить, при медленности оставить offline-путь основным.
2. Закрепить версии wheel (сейчас latest; добавить freeze-файл для воспроизводимости).
3. Дальше по бэклогу: пункт 2 «Экран метрик обучения», затем 3 (шумы измерителей), 4 (манёвры), 6 (анимация мозга — частицы вдоль LINKS, fullscreen), 5 (законы наведения APN/преследование/CLOS).

### Запуск 2 — 2026-09-17 00:35…00:55 (ночная смена)
**Сделано: пункт 2 «Экран метрик обучения и прогона» + хвосты запуска 1.**
- `web/src/LabView.tsx` — оверлей «Лаборатория» (кнопка «лаборатория» в шапке сцены): 4 canvas-графика без библиотек — промах по эпизодам обучения; 16 весов DN по эпохам (фосфор/янтарь = тангаж/рыскание); рой: лучший и средний промах по поколениям; ZEM по кадрам прогона. Экспорт CSV (обучение / рой / ZEM), пустые состояния, resize.
- `App.tsx`: коллекторы `labRef` (train/gen/zem) + version-тик; ZEM считается из кадров через новую чистую функцию `navMetrics()` (tgo/zem/γ) — дубликат в рендере убран.
- Хвосты запуска 1: multi-stage `make docker-image` собран и проверен (109 МБ, health/UI/api/run ок на 8096); wheel-версии закреплены `wheels/requirements-freeze.txt`, Dockerfile.offline ставит по freeze.
- Проверено в браузере: ZEM-график (193 точки к нулю), рой-график (10 поколений, «средний» честно показывает отставание биомух на «вдогон+вираж»), CSV-кнопки. pytest 18, tsc+build зелёные.

**Не сделано / следующему запуску:**
1. Пункты 3–5 бэклога: шумы измерителей (Scenario+UI), манёвры (ножницы/горки/барраж/комбо), законы APN/преследование/CLOS.
2. Анимация мозга: частицы вдоль LINKS + fullscreen (приоритет пользователя).
3. Мелочь: лаборатория не переживает перезагрузку страницы (можно localStorage); CSV ZEM капить 2000 точек.

### Запуск 4 — 2026-09-17 02:03…02:2x (ночная смена)
**Сделано: пункт 4 «Разнообразные манёвры цели».**
- `sim.py`: Maneuver расширен до 8 значений; `target_accel` переписан:
  - `weave_var` — переменная змейка (чирп частоты + выход на амплитуду);
  - `break` — 2.5 с прямо, затем форсированный вираж на полной перегрузке;
  - `scissors` — «ножницы»: чередование виражей влево/вправо (плавный фронт 0.35 с);
  - `dive` — горка/пике (вертикальная перегрузка, период 6 с);
  - `combo` — вираж с модуляцией + вертикальная горка в противофазе.
- Рой: `sample_generation_scenario` — первые 4 поколения простые манёвры, дальше весь арсенал (куррикулум робастности). Обучение: `_episode_scenario` вращает 6 манёвров по эпизодам.
- Фронт: селектор манёвра (8 опций), `localSim` зеркало (фолбэк), метки в `scenarioLabel`. 3D ничего не требует — трек цели рисуется как есть.
- Тесты `tests/test_maneuvers.py` (9): у каждого манёвра rollout завершается; break — «прямо → рывок»; ножницы меняют знак; горка — вертикаль; комбо — обе компоненты; переменная змейка кладкует чаще обычной; перегрузка всегда ⊥ скорости и в границах. Итого **39 passed**, tsc+build зелёные.
- Смоук: curl комбо+ПН → перехват 41.8 м; браузер: селектор «комбо», пуск, ZEM 253 м на 1.2 с.

**Самокритика:** сила манёвра n_target=4 в «combo» даёт до ~4.4g суммарной перегрузки — граница теста учла; «weave_var»-фаза честно детерминирована (без случайности внутри манёвра — рандомизация через сгенерированные сценарии).

**Не сделано / следующему запуску:**
1. Пункт 5: законы APN, чистое преследование, CLOS в `pn.py` + строки в «сравнить мозги» + опция закона в UI.
2. Пункт 6: анимация мозга — импульсы вдоль LINKS + fullscreen + пульсация.
3. Пункт 2 хвост: лаборатория — сохранение истории в localStorage (переживать перезагрузку).

### Запуск 5 — 2026-09-17 02:44…03:0x (ночная смена)
**Сделано: пункт 5 «Законы наведения».**
- `navedenie/pn.py` → 4 закона: PPN (был), **APN** (N·Vc·ω×v + N/2·a_t — эталон «с подсказкой»: знает ускорение цели), **чистое преследование** (антиэталон), **CLOS** (3-точка: держится линии «пуск — цель», нормирование на дистанцию ракета–цель).
- `Scenario.law` (pn/apn/pure/clos) + `engine.run` применяет закон в режиме «пропорциональное» (launch_p сохраняется для CLOS).
- `ScenarioIn.law` + `/api/brain/compare` принимает `laws: []` — строки законов в таблице сравнения.
- UI: селектор «Закон наведения» в «Условиях пуска» (активен в режиме ПН) с подсказкой.
- Тесты `tests/test_laws.py` (5): APN лучше ПН на манёвренной цели, чистое преследование промахивается на встречных (антиэталон подтверждён), CLOS перехватывает вдогон, все законы bounded/⊥. Итого **44 passed**, tsc+build зелёные.
- Таблица сравнения на вдогон: pn 43.1 ✓ / apn 42.5 ✓ / pure 43.8 ✓ / clos 44.5 ✓ — законы различимы.

**Самокритика:** первый вариант CLOS считал позицию ракеты как launch+r (r — это ракета→цель!) — поймал тестом «clos перехватывает вдогон», исправил передачей missile_p явно. APN использует истинное ускорение цели — в подсказке помечено «эталон с подсказкой».

**Не сделано / следующему запуску:**
1. Пункт 6: анимация мозга — импульсы вдоль LINKS + fullscreen + пульсация (приоритет пользователя).
2. Манёвры в фолбэке роя: проверка консистентности localSim против python для новых манёвров.
3. Идея: селектор закона добавить в уроки (урок «CLOS против ПН»).

### Хотфикс (утро, по фидбеку): прокрутка панелей настроек
- Поля переполняли «Условия пуска» и «Настройки контура» без прокрутки → `.fields` получил `overflow-y: auto` + `min-height: 0`. Проверено в браузере: панель скроллится, нижние поля доступны.

**Дополнение к запуску 5 (финал смены):**
- Анимация мозга сделана и проверена: импульсы бегут по проводке LINKS от реальной активности региона-источника (скорость/видимость из energies кадра), кнопка «во весь экран» (Esc — выход), средняя яркость импульсов = общая активность.
- Фолбэк законов: localSim повторяет apn/pure/clos (проверено curl на 8096 и parity).
- Манёвры: parity localSim↔python — max|Δ|=0.0 по всем 5 новым манёврам (`make check-parity`).
- Docker: финальный `muholet:latest` (linux/amd64) пересобран из финального кода, проверен (APN+шум+змейка → перехват 40.2 м), сохранён в **`muholet-amd64.tar.gz` (103 МБ)** — развёртывание: `docker load < muholet-amd64.tar.gz && docker run -p 8080:8080 muholet:latest` (интернет не нужен, `docker inspect` подтверждает linux/amd64).
- Git: 4 коммита (бэкенд+тесты / фронт / docker / roadmap+tools), рабочее дерево чистое.

**Итог ночи:** бэклог закрыт полностью (шумы, манёвры, законы, лаборатория, docker). Оставшееся на усмотрение: локальное сохранение законов в фолбэке сделано; возможные дальнейшие улучшения — импульсы мозга с цветом по типу сигнала, «урок CLOS против ПН», графики «промах vs шум» в лаборатории.
